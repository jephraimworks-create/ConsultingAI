[pytest]
# The active application and test suite live at the repository root.
# An older project copy exists under mensana-opportunity-radar/, so limiting
# collection prevents pytest from importing two files named test_app.py.
testpaths = tests
python_files = test_*.py
